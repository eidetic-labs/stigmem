# Spec-X8-Intent-Envelope Status

This file is a compatibility pointer for existing
`experimental/intent-envelope/` links.

The canonical ADR-020 status record now lives at
[`features/intent-envelope/status.md`](../../features/intent-envelope/status.md).

Current summary:

- Status: `deferred`
- Stability: `experimental`
- Default surface: `opt-in`
- Implementation path: `experimental/intent-envelope/`
- Package: none currently published

Product status, gates, history, and release-facing facts belong in the feature
record.
