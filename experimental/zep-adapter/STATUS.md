# zep-adapter — Compatibility Pointer

The canonical feature record now lives at
[`features/zep-adapter`](../../features/zep-adapter/README.md).

This directory remains the implementation location for the deferred
experimental Zep adapter. Keep code-local notes here only when they are needed
to operate the source tree; feature behavior, release status, evidence,
security posture, and changelog entries belong in the feature record.
