# cognee-adapter — Compatibility Pointer

The canonical feature record now lives at
[`features/cognee-adapter`](../../features/cognee-adapter/README.md).

This directory remains the implementation location for the dormant
experimental Cognee adapter. Keep code-local notes here only when they are
needed to operate the source tree; feature behavior, release status, evidence,
security posture, and changelog entries belong in the feature record.
