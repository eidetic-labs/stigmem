# ollama-litellm-adapter — Compatibility Pointer

The canonical feature record now lives at
[`features/ollama-litellm-adapter`](../../features/ollama-litellm-adapter/README.md).

This directory remains the legacy concept location for the superseded
Ollama/LiteLLM adapter identity. The implementation is the OpenAI-compatible
tools adapter under `experimental/openai-tools-adapter`. Keep code-local notes
here only when they explain legacy compatibility; feature behavior, release
status, evidence, security posture, and changelog entries belong in the feature
record.
