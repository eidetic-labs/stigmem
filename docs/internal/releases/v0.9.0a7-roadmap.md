# v0.9.0a7 Release Roadmap

**Status:** active
**Release owner:** maintainer
**Milestone:** [`v0.9.0a7`](https://github.com/eidetic-labs/stigmem/milestone/8)
**Tag / release:** not tagged
**Last updated:** 2026-05-23

## Release Intent

`v0.9.0a7` validates the source-attestation extraction in the alpha series.
The feature validates asserted fact `source` values against authenticated
principals and delegation lists, but that behavior must remain opt-in behind
`stigmem-plugin-source-attestation`.

This release is a validation and artifact-refresh horizon. It does not
graduate source attestation into the supported default surface, open beta/RC/GA,
or publish standalone plugin artifacts unless issue #298 is explicitly moved
into this milestone.

## Scope Summary

| Area | In scope | Out of scope | Canonical references |
| --- | --- | --- | --- |
| Release-plan recovery | Open the a7 contract from the alpha-series roadmap, feature record, and source-attestation scaffold. | Rewriting ADR decisions or treating a7 as a stable launch. | `ROADMAP.md`, `docs/internal/releases/v0.9.0-alpha-series-roadmap.md`, `features/source-attestation/` |
| Default-install boundary | Prove default installs do not enforce source-attestation checks, modify recall ranking, or apply federation source validation. | Enabling source attestation by default. | ADR-011, `features/source-attestation/status.md` |
| Plugin assertion boundary | Validate plugin-loaded assertion enforcement, warn/off behavior, source normalization assumptions, and delegation behavior. | Claiming keypair-level fact signatures or stable delegation semantics before evidence exists. | `features/source-attestation/spec.md`, `experimental/source-attestation/` |
| Recall and federation integration | Validate source-trust ranking deltas and inbound federation source validation only when the plugin gates are enabled. | Re-attesting federated facts or altering historical source provenance. | `features/source-attestation/evidence.md`, `node/src/stigmem_node/routes/facts/query.py` |
| Security and evidence | Align source-attestation records with R-22 contribution, residual spoofing risk, and release evidence. | Presenting source attestation as release supply-chain provenance. | `features/source-attestation/security.md`, `SECURITY.md`, `spec/security/threat-model.md` |
| Artifact refresh | Prepare a7 core package, SDK, OpenClaw, GHCR, docs, and GitHub release evidence if the release ships after validation. | Creating a tag, publishing artifacts, or creating an a7 release flag without maintainer clearance. | `docs/internal/release-cadence.md` |

## Detailed Scope

### Goal 1 - Open the a7 Release Contract

**Issue:** [#621](https://github.com/eidetic-labs/stigmem/issues/621)

**Objective:** establish the active release contract before implementation PRs
begin.

**Included changes:**
- Mark `v0.9.0a6` as shipped and `v0.9.0a7` as the active release horizon.
- Add this release roadmap as the canonical a7 scope document.
- Preserve the alpha deployment sequence while keeping release-specific detail
  in `docs/internal/releases/`.
- Link the existing source-attestation feature record, plugin source package,
  tests, and security posture.

**Excluded / deferred:**
- Editing ADR files.
- Opening beta, RC, GA, or stable-release milestones.
- Publishing source-attestation plugin artifacts.

**Evidence required:**
- Roadmap and release-roadmap index identify a7 as active.
- No ADR files are changed.
- Milestone and issues exist before code changes land.

**References:**
- `ROADMAP.md`
- `docs/internal/releases/v0.9.0-alpha-series-roadmap.md`
- `features/source-attestation/`
- `experimental/source-attestation/`

### Goal 2 - Reconfirm the Default-Install Boundary

**Issue:** [#622](https://github.com/eidetic-labs/stigmem/issues/622)

**Objective:** prove default installs preserve current core behavior and do not
activate source-attestation enforcement implicitly.

**Included changes:**
- Confirm fact assertion accepts existing caller-declared `source` values when
  the plugin is not registered.
- Confirm `attested` query/filter behavior remains compatible with existing
  records and does not imply plugin enforcement.
- Confirm recall ranking does not receive source-attestation score deltas
  without plugin registration and explicit gates.
- Confirm federation ingest does not reject peers based on source-attestation
  policy unless the plugin is registered and enabled.

**Excluded / deferred:**
- Removing existing `attested` storage/query compatibility fields.
- Changing core API shapes solely to make source attestation look absent.

**Evidence required:**
- Default-install tests prove no source-attestation behavior activates
  implicitly.
- Feature evidence states which compatibility fields remain in core and why.

**References:**
- `node/tests/plugins/test_source_attestation_plugin_validation.py`
- `features/source-attestation/evidence.md`
- `node/src/stigmem_node/routes/facts/query.py`

### Goal 3 - Validate Plugin Assertion Enforcement

**Issue:** [#623](https://github.com/eidetic-labs/stigmem/issues/623)

**Objective:** prove plugin-loaded assertion validation enforces the
caller-to-source boundary when operators explicitly enable it.

**Included changes:**
- Verify `experimental/source-attestation/pyproject.toml` exposes the intended
  plugin package and entry point.
- Verify the plugin manifest declares assertion, recall, federation, config,
  and health hooks.
- Validate enabled assertion enforcement rejects unauthorized source claims.
- Validate disabled gates remain no-op.
- Validate delegation behavior matches the feature spec or record any gap
  before claiming the release complete.

**Excluded / deferred:**
- Keypair-level fact signatures.
- ADR-008 graduation or stable support claims.

**Evidence required:**
- Plugin scaffold and assertion-validation tests pass.
- Security/evidence records identify any remaining delegation or normalization
  gaps.

**References:**
- `experimental/source-attestation/src/stigmem_plugin_source_attestation/manifest.py`
- `experimental/source-attestation/src/stigmem_plugin_source_attestation/config.py`
- `experimental/source-attestation/src/stigmem_plugin_source_attestation/handlers.py`
- `node/tests/plugins/test_source_attestation_plugin_scaffold.py`
- `node/tests/plugins/test_source_attestation_plugin_validation.py`

### Goal 4 - Validate Recall and Federation Behavior

**Issue:** [#625](https://github.com/eidetic-labs/stigmem/issues/625)

**Objective:** prove the non-write integration points are opt-in, deterministic,
and honest about what they do and do not attest.

**Included changes:**
- Validate recall ranking deltas apply only when source-attestation gates and
  source-trust weighting are enabled.
- Validate inbound federation source checks run only when the plugin gate is
  enabled.
- Confirm federated facts are not silently re-attested as local facts.
- Document any hook-surface limits where the current registry cannot express a
  complete source-attestation claim.

**Excluded / deferred:**
- Cross-peer fact-signature chains.
- Rewriting federation trust scoring outside the source-attestation boundary.

**Evidence required:**
- Focused tests or documented dispositions cover recall ranking and federation
  inbound behavior.
- Feature security records avoid overstating supply-chain or federation
  guarantees.

**References:**
- `features/source-attestation/spec.md`
- `features/source-attestation/security.md`
- `node/src/stigmem_node/source_trust.py`
- `node/tests/plugins/test_source_attestation_plugin_validation.py`

### Goal 5 - Align Feature Records, Security, and Public Projections

**Issue:** [#624](https://github.com/eidetic-labs/stigmem/issues/624)

**Objective:** make every non-code surface describe the same source-attestation
state.

**Included changes:**
- Align `features/source-attestation/README.md`, `spec.md`, `status.md`,
  `evidence.md`, `security.md`, and `changelog.md`.
- Align protocol, compatibility, public docs, generated docs, and security
  projections that mention source attestation.
- State clearly that source attestation validates fact authorship claims in
  plugin-loaded deployments and does not prove release artifact provenance.
- Add release-note candidates for the work that actually lands.

**Excluded / deferred:**
- Duplicating feature-owned implementation detail in `ROADMAP.md`.
- Editing ADRs.

**Evidence required:**
- Feature-record, compatibility, protocol, security, and docs validators pass.
- `SECURITY.md` and feature-level security records agree on residual risk and
  advisory policy.

**References:**
- `features/source-attestation/`
- `spec/PROTOCOL.md`
- `spec/security/threat-model.md`
- `docs/docs/operators/release-verification.md`

### Goal 6 - Prepare and Execute the a7 Release

**Issue:** [#626](https://github.com/eidetic-labs/stigmem/issues/626)

**Objective:** ship the next alpha artifact refresh only after the a7 milestone
is closed and the maintainer clears publication.

**Included changes:**
- Advance version surfaces to `0.9.0a7` / `0.9.0-alpha.7` when implementation
  and release-note work is complete.
- Dry-run package builds for the core Python packages, TypeScript SDK, and
  OpenClaw adapter.
- Validate GHCR tag posture and docs single-version posture.
- Confirm all a7 milestone issues are closed.
- Prepare signed tag, GitHub release, public signing-key asset, detached
  signature, SBOM, and image-digest evidence for maintainer approval.

**Excluded / deferred:**
- Beta, RC, GA, or stable-version artifacts.
- Standalone plugin artifact evidence unless explicitly moved from issue #298.
- Creating a tag or publishing artifacts without maintainer clearance.

**Evidence required:**
- `scripts/check_release_readiness.py --tag v0.9.0a7` passes.
- `scripts/check_version_consistency.py` passes after version bump.
- Package, docs, and release validators pass before tag/publish clearance.

**References:**
- `docs/internal/release-cadence.md`
- `docs/docs/operators/release-verification.md`
- `scripts/check_release_readiness.py`
- `scripts/check_version_consistency.py`

## Artifact Surfaces

| Surface | Expected artifact | Status | Evidence |
| --- | --- | --- | --- |
| Source-attestation plugin source | `experimental/source-attestation/` source package and feature record | Active validation | a7 implementation issues |
| PyPI | a7 core Python package artifacts | Not prepared | release closeout issue |
| npm | `@eidetic-labs/stigmem-ts@0.9.0-alpha.7` | Not prepared | release closeout issue |
| OpenClaw adapter | a7-aligned adapter artifact | Not prepared | release closeout issue |
| GHCR | `0.9.0a7` and `0.9.0-alpha.7` node image tags | Not prepared | release closeout issue |
| Docs | single-version alpha docs updated for a7 | Not prepared | docs/projection issue |
| GitHub release | `v0.9.0a7` prerelease | Not created | release maintainer |
| GPG signatures | Signed release tag and manually uploaded detached signatures | Not created | release maintainer |
| Plugin artifacts | Standalone `stigmem-plugin-source-attestation` registry artifact | Deferred unless re-scoped | issue #298 |

## Security and Risk Posture

`v0.9.0a7` remains an alpha release. Source attestation is experimental and
opt-in. The main security invariant is boundary honesty: default installs must
not enforce source-attestation policy, plugin-loaded deployments must enforce
only the gates operators enable, accepted facts must not be presented as
locally re-attested, and docs must not present source attestation as release
supply-chain provenance.

Source attestation contributes to R-22 because operators must trust the code
that performs source binding, but R-22 is not closed by this feature. Release
supply-chain evidence remains owned by the release process: signed tags,
detached artifact signatures, SBOMs, image digests, provenance, and Rekor /
Sigstore verification where applicable.

Security publication policy remains unchanged: Critical/High disclosures use
GHSA where applicable; Medium/Low findings remain in `SECURITY.md` unless a
documented risk-profile, reporter-coordination, or downstream-compliance
carve-out applies.

## Evidence Gates

| Gate | Required evidence | Status |
| --- | --- | --- |
| Release contract | a7 roadmap, milestone, and issues exist; a6 marked historical. | Complete |
| Default boundary | Default installs do not enforce source attestation or ranking. | Complete |
| Plugin assertion boundary | Plugin-loaded assertion enforcement, disabled gates, and delegation disposition are tested. | Complete |
| Recall/federation behavior | Ranking and inbound federation behavior are tested or explicitly dispositioned. | Complete |
| Security posture | R-22 contribution and residual risk are stated consistently. | Complete |
| Projection alignment | Feature records, security projections, public docs, and changelog surfaces agree. | Complete |
| Artifact readiness | Version surfaces, package dry-runs, docs checks, and release readiness checks pass. | Pending |

## Release Notes Candidates

- Validate `stigmem-plugin-source-attestation` as the opt-in alpha boundary for
  source-attestation behavior.
- Keep default installs free of source-attestation enforcement unless the plugin
  is registered and enabled.
- Confirm assertion validation, recall ranking signals, and federation inbound
  checks are controlled by explicit plugin gates.
- Clarify that source attestation validates fact authorship claims in
  plugin-loaded deployments and does not prove release artifact provenance.
- Align source-attestation feature records, security posture, public docs, and
  release evidence for the a7 alpha horizon.

## Deferred / Follow-Up Work

| Item | Disposition | Target |
| --- | --- | --- |
| Standalone plugin package publication | Deferred unless issue #298 is explicitly moved into a7. | issue #298 / plugin launch train |
| Keypair-level fact signatures | Not part of this alpha validation release. | future source-attestation hardening |
| Full delegation conformance | Validate what exists in a7; defer gaps with explicit issues. | a7 issue or future hardening |
| Multi-tenant isolation feature launch | Next planned alpha extraction horizon. | `v0.9.0a8` |
| ADR-008 graduation | Not part of alpha extraction. | post-GA / ADR-008 gates |
| Hardened-core, beta, RC, GA | Future gates only. | future release roadmaps |

## Historical Disposition

Not yet shipped. Complete after `v0.9.0a7` receives maintainer publication
clearance, tag, artifact publication, and release evidence.
